<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./css/general.css" />
    <link rel="stylesheet" href="./css/header.css" />
    <link rel="stylesheet" href="./css/loginStyles.css" />
    <title>Log-In</title>
  </head>
  <body>
    <%- include('header', {url : url}) %>
    <div class="content">
      <h1 class="formTitle fullWidth">Log In</h1>
      <form class="formContainer " action="/login" method="POST">
        <div class="inputContainer">
          <label class="normalLabel" for="username">Username</label>
          <input
            class="smallInput"
            id="username"
            name="username"
            placeholder="test"
            type="text"
          />
          <p class="error"></p>
        </div>
        <div class="inputContainer">
          <label class="normalLabel" for="password">Password</label>
          <input
            class="smallInput"
            id="password"
            name="password"
            type="password"
            placeholder="123"
          />
          <p class="error"></p>
        </div>
        <button class="submitButton">Log In</button>
        <% if(failure) { %>
        <div class="bottomErrorWrapper">
          <p class="frontError"><span class="errorText">Invalid Log-In:</span> Please try again or <a href="/signup">Sign Up</a></p>
        </div>

        <%}%>
        <div class="infoWrapper">
          <p class="info">To test app without making account:</p>
          <div class="infoInnerWrapper">
            <p class="info">Username: test</p>
            <p class="info">Password: 123</p>
          </div>
        </div>
      </form>
    </div>
  </body>
</html>
